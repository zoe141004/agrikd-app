# AgriKD Jetson — Python Dependencies
# Used by: system pip install (setup_jetson.sh step 7)
#
# NOTE: TensorRT and PyCUDA are provided by the NVIDIA JetPack SDK.
#       Do NOT add them here.
# NOTE: PyQt5 is installed via apt (python3-pyqt5) for GPU acceleration.
#       Do NOT add it here.
#
# setup_jetson.sh installs newer patched versions for the host.
# This file pins minimum-compatible versions for reference.

numpy==1.24.4
opencv-python-headless==4.8.1.78
flask==3.1.3
waitress==3.0.2
requests==2.32.4