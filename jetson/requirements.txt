# AgriKD Jetson — Python Dependencies
# Used by: Docker build (Dockerfile) and system pip install (GUI mode)
#
# NOTE: TensorRT and PyCUDA are provided by the NVIDIA JetPack SDK / L4T Docker image.
#       Do NOT add them here.
# NOTE: PyQt5 is installed via apt (python3-pyqt5) for GPU acceleration on host.
#       It is NOT needed in Docker (headless mode).
#
# COMPATIBILITY: All versions MUST work with both Python 3.8 (Docker/L4T) and 3.10 (host).
#   - Docker container uses Python 3.8 (from L4T base image, required for TensorRT bindings)
#   - Jetson host / dev / CI use Python 3.10 (project standard)
#   - See Dockerfile header comments for why Docker cannot use 3.10.

numpy==1.24.4
opencv-python-headless==4.8.1.78
flask==3.0.3
waitress==2.1.2
requests==2.31.0