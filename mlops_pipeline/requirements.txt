# AgriKD MLOps Pipeline - Full requirements (local dev)
# =============================================
# Install: pip install -r requirements.txt
#
# torch & torchvision cài riêng (cần chỉ định index URL):
#   CPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# For CI, use split requirements instead:
#   requirements-convert.txt  — ONNX + TFLite conversion only
#   requirements-evaluate.txt — full evaluation + benchmarking

-r requirements-convert.txt
-r requirements-evaluate.txt
